"use strict";

(function () {
    //include elements.js

    flottplot.Flottplot.registerTag("fp-cursors", FPCursors, false);
    flottplot.Flottplot.registerTag("fp-overlay", FPOverlay, false);
    flottplot.Flottplot.registerTag("fp-stack", FPStack, true);

})();

